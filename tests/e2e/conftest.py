"""E2E shared fixtures.

Re-exports the same app/repo/client fixtures from the API integration suite
so E2E tests can reuse them naturally (pytest discovers fixture functions
imported into the conftest module namespace).
"""

